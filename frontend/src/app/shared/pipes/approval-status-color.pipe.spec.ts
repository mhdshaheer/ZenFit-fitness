import { ApprovalStatusColorPipe } from './approval-status-color.pipe';

describe('ApprovalStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ApprovalStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
