# This file documents the values consumed by environment.ts files.
# Copy these into environment.ts / environment.prod.ts or
# generate runtime configuration via a build script as needed.
API_URL=http://localhost:5005/api/v1
SOCKET_URL=http://localhost:5005
STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXXXXXX
